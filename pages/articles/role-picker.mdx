import Layout from "@/components/layout"
import HelloWorld from "@/components/hello-world"

# A Draggable Role Picker Demo for Party Animals

This is a mdx `test` file.

<HelloWorld />

```js
function hello() {
  console.log("hello")
}
```

export default ({ children }) => <Layout>{children}</Layout>
